import { ChannelsClient } from './channels-client'

export const metadata = {
  title: 'Channels | ClawHub',
  description: 'Manage messaging channels and integrations',
}

export default function ChannelsPage() {
  return <ChannelsClient />
}
